import { Title } from '@angular/platform-browser';

export interface Blog {
  id?: number,
  Titulo?: string,
  Subtitulo?: string,
  Resumen?: string,
  ImagendelBlog?: string,
  VideodelBlog?: string
}
