import { Title } from '@angular/platform-browser';

export interface Field {
  id?: number,
  Name?: string,
  Resumen?: string,
  Descripcion?: string,
  IconoField?: string,
  ImagenField?: string,
  Precio1?: string,
  Precio2?: string
}
